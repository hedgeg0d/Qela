// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 25;
fn mul2(a int, b int) int { return a * b; }
fn sub2(a int, b int) int { return a - b; }
fn mix5(a int, b int, c int, d int, e int) int { return a + b - c + d - e; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 0x3;
    var v1 int = 3;
    var v2 int = 10;
    var sv0 str = "42";
    var sv1 str = """x
yz""";
    var fv double = 100.0;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0x0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    v1, gv = gv, v1;
    gv = false;
    v1 = gv;
    v1 = v0;
    gv *= (((sub2(false, true) as i8) << a[((sv0).len & 0x3)]) << ((a[(-1 & 3)] as i8) in 0..((false & 15) + 1)));
    v0 = (str_eq("xyzzy", "foobar") >= (((("abc")[0..2])[0] < v0) >= str_eq((sv0)[0..2], (("qwerty" + sv0))[0..2])));
    v0 = (((((gv as u16) <= ((sv0)[0..2]).len) && (((sv0)[0..2]).len > (-0))) as i32) as i8);
    v0 >>= ((((gv as i8) in 0..(((false < 0xa) & 15) + 1)) if ((((('g' >> gv) <= (-gv)) if (v2 <= '\t') else (-(v2 >= gv))) as i16) > true) else 0xa) as u8);
    gv = (!(((fv - 100.0) - fv) as i64));
    p.y = (((('b' >> 42) as i16) as u16) in 0..((((mul2(v1, gv) as i8) >= ((false ^ '2') != (-true))) & 15) + 1));
    v0 = str_eq(sv1, ("abcd")[1..4]);
    write_str(STDOUT, "${v2=}\n");
    return ((str_eq(("abc")[0..2], sv1) | 'g') == ("xyzzy").len);
}
