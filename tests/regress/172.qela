// expect-exit: 246
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -12;
fn max2(a int, b int) int { if (a > b) { return a; } else { return b; } }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn min2(a int, b int) int { if (a < b) { return a; } else { return b; } }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 9;
    var v1 int = 1;
    var v2 int = 6;
    var sv0 str = "abc";
    var sv1 str = "foobar";
    var fv double = 2.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    e = E.C(((!(-(fv as i64))) != (((!gv) + ('a' <= '2')) & ((gv as i8) <= (("xyzzy")[1..5])[1]))), (((fv > (fv / 2.0)) as i32) / (((((sv0)[1] % (((-993 % ((-213 & 15) + 1)) & 15) + 1)) > (gv << v2)) & 15) + 1)));
    match (e) {
        A => {
            return ((-(p.y & (!255))) >> false);
        }
        B(mb0) => {
            return p.y;
        }
        C(mb0, mb1) => {
            return (((-10) as i16) - mb1);
        }
    }
    match (e) {
        A => {
            return 42;
        }
        B(mb0) => {
            return (((((sv1).len ^ (fv as i64)) == (('e' as u32) as u32)) ^ (fv <= ((3.75 + 2.5) * (2.5 / 0.25)))) - mb0);
        }
        C(mb0, mb1) => {
            return ((false ^ p.y) & mb1);
        }
    }
    fv = (10.5 / 2.0);
    if ((gv == 703)) {
        match (e) {
            A => {
                return (((("abc")[0..3])[1] + v1) < (((42 % ((-483 & 15) + 1)) >> 0) - v1));
            }
            B(mb0) => {
                return (!(((false - -946) ^ (gv - 0)) * bag_sum(b)));
            }
            C(mb0, mb1) => {
                return ((127 < (!v1)) - mb1);
            }
        }
        return ((!((false != 100) % (((v0 & false) & 15) + 1))) as u32);
    } else {
        e = E.C((((fv + (fv / 10.5)) / 1.7) as i64), ((p.x + '\t') >> (fv as i64)));
        v2, gv = gv, v2;
    }
    e = E.B(a[(nest_sum(Nest{tag: (((100 << (2 + gv)) >> (v1 as u8)) > ((a[('b' & 3)] - str_eq(("xyzzy")[0..4], ("hello")[1..3])) + ((false >> 'a') / (((255 % ((-374 & 15) + 1)) & 15) + 1)))), p: Pair{x: pair_sum(Pair{x: v0, y: (-(sv1)[0])}), y: ((((-708 >= -1) << (true % ((383 & 15) + 1))) << ((0 >> true) | (--233))) < 2)}}) & 3)]);
    a[(349 & 3)] = (-((("hello")[1..5])[0..2])[0]);
    sv1 = ("foobar")[0..2];
    v2 -= gv;
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((sv0)[0] > min2(v2, 1)) as u8) == ((("xyz")[0..2])[0] / ((465 & 15) + 1)));
}
