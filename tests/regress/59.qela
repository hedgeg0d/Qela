// expect-exit: 42
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn sub2(a int, b int) int { return a - b; }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 8;
    var v1 int = 4;
    var v2 int = 2;
    var sv0 str = "zz";
    var sv1 str = "42";
    var fv double = 0.125;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    match (e) {
        A => {
            return (42 as u32);
        }
        B(mb0) => {
            return (pair_sum(Pair{x: '\n', y: ("qwerty").len}) * mb0);
        }
        C(mb0, mb1) => {
            return ((("abc")[0..3])[1] * mb1);
        }
    }
    e = E.C((true | (((true > v1) as double) as i64)), ((-(p.y <= (10.5 >= fv))) >> (((-972 != v1) <= (false == v0)) as i32)));
    n.p.x = ((v2 - ((-409 + 0) - ('a' / (('f' & 15) + 1)))) * ((-pair_sum(Pair{x: true, y: 956})) >= ((-1.5 / -1.5) as i64)));
    if (((("xyz")[0..2]).len < false)) {
        v0, v1 = (("zz")[0..2]).len, ((mix6('a', (true <= 2), ('e' as u8), v2, (sv0)[1], str_eq(("xyz")[0..3], "aa")) as u8) ^ ((sv1).len as u32));
    } else {
        n.tag = ((((true >> 232) >> (true >= 0)) as double) as i64);
        match (e) {
            A => {
                return ((((1.0 / -2.0) / 1.7) as i64) & ((-('0' >= -86)) != (('1' ^ 1) < (127 as i8))));
            }
            B(mb0) => {
                return ((("abcd")[1] == ((false / ((-892 & 15) + 1)) as u32)) | mb0);
            }
            C(mb0, mb1) => {
                return (((((100.0 / 0.5) + (fv / 2.5)) + fv) as i64) & mb1);
            }
        }
    }
    match (e) {
        A => {
            return true;
        }
        B(mb0) => {
            return ((((v2 as double) as i64) as i32) != (-(('3' < false) >> v2)));
        }
        C(mb0, mb1) => {
            return '1';
        }
    }
    v2 = ((((sv0)[0..2])[0..2])[0..2])[1];
    i0 = 0;
    while ((i0 < 3)) {
        i0 = (i0 + 1);
        a[(((!(('\t' as u16) >= nest_sum(Nest{tag: (2 != (((1.0 + 0.3) + (0.5 + fv)) as i64)), p: Pair{x: ((((0 as i16) >> (265 + v2)) * v0) < 'c'), y: str_eq(sv1, sv0)}}))) != ((mix6(127, true, -1, -727, 42, false) / (((true % (('0' & 15) + 1)) & 15) + 1)) - 'a')) & 3)] = ((p.y as double) >= (((-'0') / ((('c' as i32) & 15) + 1)) as double));
    }
    e = E.C((!(false <= (-tag_e(e)))), p.x);
    v0 = (str_eq(("abcd")[1..4], ("abcd")[1..3]) ^ (((10 / (('3' & 15) + 1)) == ('c' | 'b')) as i16));
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((fv - 100.0) / 3.75) as i64) as u8);
}
