// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn max2(a int, b int) int { if (a > b) { return a; } else { return b; } }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 3;
    var v1 int = 0;
    var v2 int = 10;
    var sv0 str = "abcd";
    var sv1 str = "xyz";
    var fv double = 2.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    sv1 = (sv0)[0..2];
    v2 = ((!((0 as i8) - ('2' << v1))) & (("no")[1] as i16));
    v0 = v2;
    if ((wide_sum(w) < (str_eq(sv0, "xyz") >= (((54 < v1) > (v2 % ((2 & 15) + 1))) < 1)))) {
        v1 = ((-689 != (!('2' as u16))) != pair_sum(Pair{x: a[((--279) & 3)], y: (sv0)[0]}));
        return (-((1.5 as i64) <= 10));
    } else {
        e = E.B(((sv0).len as u32));
    }
    w = wide_make(((974 & v2) / (((-564 <= ((v2 >= 0) as i8)) & 15) + 1)), false, (-206 as u16));
    e = E.B((-(((false / ((false & 15) + 1)) as i32) ^ (('2' <= 255) - -378))));
    match (e) {
        A => {
            return (pair_sum(Pair{x: ((true as double) < (fv + fv)), y: ((-v2) as i16)}) as u8);
        }
        B(mb0) => {
            return (!tag_e(e));
        }
        C(mb0, mb1) => {
            return (tag_e(e) as i8);
        }
    }
    n.p.x = true;
    n.p.x = ((((0.25 / 1.7) / -0.5) + ((-245 & '3') as double)) != (fv / -0.5));
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return v2;
}
