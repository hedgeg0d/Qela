// expect-exit: 1
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -17;
fn sub2(a int, b int) int { return a - b; }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 3;
    var v1 int = 4;
    var v2 int = 1;
    var sv0 str = "hi";
    var sv1 str = "foobar";
    var fv double = 3.75;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    gv = (((-(!gv)) & ('d' >= (127 as u32))) << ((p.y % (((100 + false) & 15) + 1)) as i16));
    i0 = 0;
    while ((i0 < 4)) {
        e = E.C(sub2((sub2((-489 as u32), gv) as u32), (((gv | true) <= v2) + (-2.0 != (fv / 3.75)))), a[(tag_e(e) & 3)]);
        match (e) {
            A => {
                return (-(("abc")[0..2]).len);
            }
            B(mb0) => {
                return (((((fv / -2.0) / 1.7) - fv) == fv) * mb0);
            }
            C(mb0, mb1) => {
                return ((((-(true <= 157)) & (a[('1' & 3)] * (false == 234))) >= ((fv * (3 as double)) < (fv + (2 as double)))) - mb1);
            }
        }
        a[i0] = (bag_sum(b) | (-((fv * fv) > -1.5)));
        i0 = (i0 + 1);
    }
    match (e) {
        A => {
            return ((((v1 as double) / 10.5) as i64) ^ 'f');
        }
        B(mb0) => {
            return ((-(((3 * gv) << 100) as i32)) + mb0);
        }
        C(mb0, mb1) => {
            return '\n';
        }
    }
    gv |= (gv as u16);
    sv1 = ("abc")[0..2];
    sv1 = "abcd";
    n.p.x = ((((sv1)[0] <= opt_d(D.None)) + v0) as u32);
    i0 = 0;
    while ((i0 < 2)) {
        i0 = (i0 + 1);
        gv, v1 = (!'g'), (((p.y | '3') + false) / (((((gv << 2) * mix6(false, 'b', 790, true, true, true)) << ("42")[0]) & 15) + 1));
    }
    p.y = (((gv >= (127 >> 'e')) / (((gv != (false >= true)) & 15) + 1)) as u16);
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((-355 < ((v1 != gv) * (true * v1))) >= (a[((100 >= false) & 3)] << (('\t' - '\t') + (v2 >> 'a'))));
}
