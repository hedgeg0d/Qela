// expect-exit: 224
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn max2(a int, b int) int { if (a > b) { return a; } else { return b; } }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn fact(n int) int { if (n <= 1) { return 1; } return n * fact(n - 1); }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 6;
    var v1 int = 4;
    var v2 int = 9;
    var sv0 str = "hello";
    var sv1 str = "abc";
    var fv double = -2.0;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    a[(fact(((true - (fv != (10.5 + -1.5))) & 15)) & 3)] = true;
    match (e) {
        A => {
            return (v2 + ((4 - (v0 - -577)) / ((mix6((true < 10), (v0 >> '2'), (fv as i64), (true as u16), (false as u8), true) & 15) + 1)));
        }
        B(mb0) => {
            return ((((p.y + false) != (-(3 as u32))) >= ('3' == (pair_sum(p) as u8))) + mb0);
        }
        C(mb0, mb1) => {
            return (((1.0 / 0.25) <= 0.3) - (((sv1).len < (!-298)) as u8));
        }
    }
    v2 ^= bag_sum(b);
    e = E.B(v0);
    e = E.C(a[(max2((true >= (1 & true)), (!(fv as i64))) & 3)], (gcd2((false & 15), ((("aa")[0..2]).len & 15)) > (((v1 as i32) | (fv as i64)) / (((str_eq((sv0)[0..2], ("hello")[0..4]) * (true == -690)) & 15) + 1))));
    v0 = (!(!((true + v0) <= (fv as i64))));
    a[(((((100.0 < fv) == 766) > (mix6(0, '1', v0, 127, 255, 'e') as u16)) != ((-'\t') < ((sv1)[0..2])[1])) & 3)] = (fact(((!p.x) & 15)) - (((100 << 972) / ((gcd2((false & 15), ('d' & 15)) & 15) + 1)) + true));
    p.x = v0;
    i0 = 0;
    while ((i0 < 4)) {
        match (e) {
            A => {
                return (true as i8);
            }
            B(mb0) => {
                return ((((1.0 <= (100.0 / 1.0)) as u16) / ((((('\n' + 951) != ('c' - -1)) != p.x) & 15) + 1)) + mb0);
            }
            C(mb0, mb1) => {
                return ((v0 - (((255 * 'g') == 'a') + v1)) + mb1);
            }
        }
        e = E.B((("hello")[0..2])[1]);
        a[i0] = ((!(str_eq(("foobar")[2..6], sv0) | (!'3'))) != (!(-(v1 >= v1))));
        i0 = (i0 + 1);
    }
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (-('2' >> v2));
}
