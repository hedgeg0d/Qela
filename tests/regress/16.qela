// expect-exit: 1
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -12;
fn twice(n int) int { return n + n; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn min2(a int, b int) int { if (a < b) { return a; } else { return b; } }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 5;
    var v1 int = 10;
    var v2 int = 2;
    var sv0 str = "zz";
    var sv1 str = "42";
    var fv double = 10.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    if (((gv <= gcd2((((gv > gv) & (fv > -2.0)) & 15), (str_eq(("no")[0..2], "no") & 15))) >= 42)) {
        sv0 = "no";
    } else {
        fv = (((0.1 - (3.75 / 10.5)) - (v0 as double)) + (((127 > 'b') >> (10 as i16)) as double));
        i1 = 0;
        while ((i1 < 2)) {
            i1 = (i1 + 1);
            v1 = min2(min2(false, ((-534 == '1') != gv)), (a[((fv as i64) & 3)] & gv));
            i2 = 0;
            while ((i2 < 2)) {
                i2 = (i2 + 1);
                match (e) {
                    A => {
                        return ((((-'0') & (sv1)[1]) as u32) >> ("abcd").len);
                    }
                    B(mb0) => {
                        return ((!(((-true) <= (1 < 663)) - 448)) - mb0);
                    }
                    C(mb0, mb1) => {
                        return (fv as i64);
                    }
                }
            }
            if ((i1 == 1)) {
                break;
            } else {
            }
        }
    }
    b = Bag{p: (((("42")[0..2])[0] as u16) * (((3 - 255) << v0) << (-("abcd")[1]))), q: ((((2.0 * 3.75) as i64) | (-'a')) as i32), r: str_eq(sv1, sv1), s: false};
    a[((2.5 as i64) & 3)] = (sv0)[1];
    v0 = ((sv1)[0] <= ((-(v1 <= gv)) & ('g' << (true > true))));
    p.x = (((!(--1)) * gv) - (((gv as u8) % (((--760) & 15) + 1)) - (false > false)));
    gv = (p.x > (min2(str_eq(sv0, ("hello")[2..4]), (false <= v2)) * ((10 / (('e' & 15) + 1)) < (-315 >= -746))));
    p.y = ((((3 >> gv) as u32) <= p.y) != (((true as i8) << 127) ^ (str_eq("foobar", "zz") - 10)));
    gv = (((min2('1', '2') << (v1 >> 'a')) | (-(false % (('f' & 15) + 1)))) * 320);
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (str_eq(sv0, "xyz") as i8);
}
