// expect-exit: 4
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 0;
fn add2(a int, b int) int { return a + b; }
fn sub2(a int, b int) int { return a - b; }
fn mul2(a int, b int) int { return a * b; }
fn fact(n int) int { if (n <= 1) { return 1; } return n * fact(n - 1); }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 1;
    var v1 int = 6;
    var v2 int = 4;
    var sv0 str = "xyz";
    var sv1 str = "abcd";
    var fv double = 2.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    v1, v0 = v0, v1;
    match (e) {
        A => {
            return v2;
        }
        B(mb0) => {
            return ((true - 3) & mb0);
        }
        C(mb0, mb1) => {
            return (str_eq(((("abc")[0..2])[0..2])[0..2], "qwerty") - mb1);
        }
    }
    n.tag = (((gv << ('e' <= '\t')) / ((false & 15) + 1)) as i32);
    sv0 = ("xyzzy")[1..3];
    v1 = (a[((1.5 == (fv / -2.0)) & 3)] + (((-v1) as i8) < (v0 / (((--417) & 15) + 1))));
    match (e) {
        A => {
            return pair_sum(Pair{x: (gv / ((((true - 1) as i8) & 15) + 1)), y: (v2 << (('e' + 113) + (120 as i16)))});
        }
        B(mb0) => {
            return (((0.1 as i64) != (a[(true & 3)] ^ ('e' as i16))) >= (((42 * 'a') << (!'f')) + ((gv as u32) as u8)));
        }
        C(mb0, mb1) => {
            return (((((2.0 / 2.5) as i64) + 3) >= false) + mb1);
        }
    }
    v1 = (('e' >> ((v0 % ((42 & 15) + 1)) as u16)) as u16);
    a[(((((true & 100) & (-486 as i16)) > p.x) & (a[((("aa")[0..2])[1] & 3)] >> (-gv))) & 3)] = (!(-(('f' as u16) * (932 & 10))));
    v0 = ((((false as u32) & ('e' as u16)) * ((0 % (('d' & 15) + 1)) != gv)) * (a[(('b' != gv) & 3)] * ((gv as double) as i64)));
    gv, v2 = (((!(196 * -608)) as u8) != ('\n' <= ((-388 as i16) + (!gv)))), p.y;
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((str_eq(sv1, sv0) | (p.y as i32)) < (p.x + 'b'));
}
