// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 6;
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn sum3(a int, b int, c int) int { return a + b + c; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 7;
    var v1 int = 10;
    var v2 int = 1;
    var sv0 str = "hi";
    var sv1 str = "abcd";
    var fv double = 10.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    v2 /= ((tag_e(e) & 15) + 1);
    v1, v0 = 1, gv;
    if ((v2 < (-str_eq("abc", sv1)))) {
        i1 = 0;
        while ((i1 < 1)) {
            i1 = (i1 + 1);
            gv = v1;
            if ((i1 != 0)) {
                continue;
            } else {
            }
        }
    } else {
        gv = opt_d(D.None);
    }
    b = Bag{p: diff4(((sv1).len % (((v2 - (true >> 42)) & 15) + 1)), (100.0 as i64), sum3((false as i32), 255, ((776 as u8) < (gv != 70))), (-((-3) as u8))), q: ((((fv * 1.0) / 0.5) < ((-2.0 * 2.0) + -0.5)) as i32), r: (a[((("42")[0..2])[0] & 3)] | false), s: (a[(sum3((v1 | gv), str_eq(("xyzzy")[1..4], ("abcd")[0..2]), (!-1)) & 3)] ^ false)};
    match (e) {
        A => {
            return (!('c' as u8));
        }
        B(mb0) => {
            return (-(-((-gv) * (v0 < 1))));
        }
        C(mb0, mb1) => {
            return (((!(('e' as u32) as u16)) as u32) + mb1);
        }
    }
    sv1 = sv0;
    p.x = (3 >= a[((gv << p.y) & 3)]);
    n.p.x = ((-v1) * (((("hello")[2..4])[1] as i32) >> ((2 as u32) / (('e' & 15) + 1))));
    v1, v2 = (((sv0).len as double) > 0.25), gv;
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (((true as double) / 0.125) as i64);
}
