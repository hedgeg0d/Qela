// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 27;
fn max2(a int, b int) int { if (a > b) { return a; } else { return b; } }
fn sum3(a int, b int, c int) int { return a + b + c; }
fn mul2(a int, b int) int { return a * b; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 2;
    var v1 int = 7;
    var v2 int = 6;
    var sv0 str = "hi";
    var sv1 str = "aa";
    var fv double = 1.7;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    gv ^= ((-(('\t' as i32) ^ '3')) - (fv as i64));
    v2 = ((fv as i64) < ((max2('a', 2) as u16) as i16));
    v0 = ((((v2 | -277) * 520) <= (('\n' as u32) - p.x)) % (((((false <= 'a') == ('1' ^ true)) << (!(755 as u16))) & 15) + 1));
    v0, v2 = (("abc")[0..3])[1], ((gv < (p.y != (!'\t'))) * (((false < 0) as i16) > ((v1 ^ 100) / ((778 & 15) + 1))));
    match (e) {
        A => {
            return str_eq((("xyz")[0..3])[0..2], sv0);
        }
        B(mb0) => {
            return (((!(((sv1)[0..2])[0] << (3 > 127))) | false) + mb0);
        }
        C(mb0, mb1) => {
            return (((tag_e(e) < (-('a' >> 1))) == (("xyz")[0..2]).len) | mb1);
        }
    }
    if (((true | (a[(p.y & 3)] + (-a[(v2 & 3)]))) > (-pair_sum(p)))) {
        n = Nest{tag: (!'b'), p: Pair{x: (!(((true >> 'b') * ('a' << v0)) % ((((-gv) % (((false == -635) & 15) + 1)) & 15) + 1))), y: ((((-300 >= v1) == p.y) as i16) % ((v0 & 15) + 1))}};
        a[((((p.y / (((1 / ((196 & 15) + 1)) & 15) + 1)) ^ ((0 / ((true & 15) + 1)) as u16)) ^ ((-(gv | 1)) / ((10 & 15) + 1))) & 3)] = (mul2((-(true >> 1)), (str_eq(sv1, (("foobar")[2..4])[0..2]) as u8)) >= (((818 as i8) ^ ('d' > '2')) < gv));
        return 0;
    } else {
        a[((((('c' as i16) < (--967)) as u16) as i16) & 3)] = mul2(((("hello")[1..5]).len / (((!(true as i32)) & 15) + 1)), (((127 != false) >> (fv as i64)) < (gv ^ (100 - 255))));
        v0 = gv;
    }
    a[(((((-1.5 / 2.0) / 2.0) * (fv * (0.125 * fv))) as i64) & 3)] = str_eq("xyzzy", "aa");
    n = Nest{tag: (((mul2(27, false) > (255 / ((83 & 15) + 1))) as i16) | ((("xyzzy")[1..5])[0..2])[0]), p: Pair{x: (-557 as i8), y: ((2.5 * fv) == fv)}};
    fv = fv;
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (mul2(sum3((v0 == gv), ('f' >> '0'), (-v1)), gv) > (!((fv / -2.0) == -0.5)));
}
