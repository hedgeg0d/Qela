// expect-exit: 102
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn mix5(a int, b int, c int, d int, e int) int { return a + b - c + d - e; }
fn sub2(a int, b int) int { return a - b; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn zero() int { return 3; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 6;
    var v1 int = 7;
    var v2 int = 2;
    var sv0 str = "abc";
    var sv1 str = "hello";
    var fv double = 1.7;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    e = E.C((((v2 / ((zero() & 15) + 1)) >> ((127 - true) as u8)) <= (((fv as i64) * (0 + true)) - ((343 <= -983) as i32))), v2);
    v0, v2 = ((sv0).len != 2), (('b' >= (sv1).len) as u32);
    v1 = ((0.1 as i64) as i16);
    v2 = '\t';
    v1 = (!'b');
    v0 /= ((((v2 << (false & ('2' >= 875))) > ((('d' + false) / ((opt_d(D.Some(false)) & 15) + 1)) as i16)) & 15) + 1);
    p.x = ((0.125 as i64) as u16);
    fv = (fv / 2.5);
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (false ^ 'f');
}
