// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 15;
fn sum3(a int, b int, c int) int { return a + b + c; }
fn mul2(a int, b int) int { return a * b; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn fact(n int) int { if (n <= 1) { return 1; } return n * fact(n - 1); }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 0;
    var v1 int = 1;
    var v2 int = 7;
    var sv0 str = "hi";
    var sv1 str = "zz";
    var fv double = 0.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    n.tag = 'f';
    gv = ("no").len;
    match (e) {
        A => {
            return ((((-807 >> v0) << (!-469)) >= 100) * (((fv < fv) < ('1' - false)) ^ (sv1)[1]));
        }
        B(mb0) => {
            return str_eq(sv0, sv1);
        }
        C(mb0, mb1) => {
            return (("qwerty")[1..5])[1];
        }
    }
    if ((((!str_eq(("foobar")[1..6], sv1)) * ('0' == v1)) >= (-(-'d')))) {
        i1 = 0;
        while ((i1 < 3)) {
            i1 = (i1 + 1);
            sv0 = (("xyz")[0..2])[0..2];
            p.x = (a[(((v0 & gv) > str_eq("xyz", "no")) & 3)] > (('g' ^ (-10)) + (!'\n')));
            gv = (((tag_e(e) as u16) % ((100 & 15) + 1)) / ((((-65 <= (true >> 42)) < (-sum3('2', v2, 954))) & 15) + 1));
        }
    } else {
        gv *= (!v1);
        p = pair_swap(p);
        return ((((100 * 448) as u8) - ((--756) == (!'\t'))) as i8);
    }
    v0 <<= 901;
    match (e) {
        A => {
            return (false > str_eq(((("foobar")[0..5])[0..2])[0..2], (((("qwerty")[2..5])[0..2])[0..2])[0..2]));
        }
        B(mb0) => {
            return ((fv / 0.25) as i64);
        }
        C(mb0, mb1) => {
            return (42 - mb1);
        }
    }
    v0 &= ((!(2 - mul2(gv, v2))) + ((!(-'g')) as u8));
    e = E.B((p.y % (((0.25 <= fv) & 15) + 1)));
    fv = fv;
    v0 = (((gv < '2') as u16) > ((("abcd")[1..4]).len ^ fact(((true == 'f') & 15))));
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (((v1 * (("qwerty")[1..4])[0]) as u32) << '\n');
}
