// expect-exit: 98
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 15;
fn sum3(a int, b int, c int) int { return a + b + c; }
fn mul2(a int, b int) int { return a * b; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn fact(n int) int { if (n <= 1) { return 1; } return n * fact(n - 1); }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 0;
    var v1 int = 1;
    var v2 int = 7;
    var sv0 str = "hi";
    var sv1 str = "zz";
    var fv double = 0.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0x0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    n.tag = 'f';
    gv = ("no").len;
    sv1 += "qwerty";
    v2 = v1;
    sv1 += ((("foobar")[0..4] + ("""ab
cd""")[2..4]))[0..2];
    match (e) {
        A => {
            return ((("""ab
cd""")[0..2])[0..2])[1];
        }
        B(mb0) => {
            return (((-737 ^ ("hello")[0]) if ((((mul2((gv in a), (gv - '1')) >= (p.y as i16)) && ((p.y as i16) < 0x2a)) as i32) == ((!fact(('\n' & 15))) in a)) else ('g' ^ (-10))) << (!-469));
        }
        C(mb0, mb1) => {
            return ((!(-("no").len)) ^ mb1);
        }
    }
    e = E.B(v0);
    gv = (((!(v1 & -202)) != gv) / (((((!3) in a) < (-sum3('2', v2, 954))) & 15) + 1));
    sv0 = (("hi" + ("xyz")[0..2]) + sv1);
    a[(v1 & 3)] = ((((--756) - (true == '\n')) as i32) & (("hello")[0..4]).len);
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return str_eq(sv1, sv0);
}
