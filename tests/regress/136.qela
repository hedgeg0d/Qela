// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn zero() int { return 3; }
fn add2(a int, b int) int { return a + b; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 9;
    var v1 int = 0;
    var v2 int = 3;
    var sv0 str = "abcd";
    var sv1 str = "42";
    var fv double = -0.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    n = Nest{tag: 10, p: Pair{x: (3.75 < (((2.0 - fv) + (fv * 0.5)) * ((v2 >> v1) as double))), y: v0}};
    match (e) {
        A => {
            return ((-p.x) as u32);
        }
        B(mb0) => {
            return ((-(!(-(!true)))) & mb0);
        }
        C(mb0, mb1) => {
            return ((fv as i64) & mb1);
        }
    }
    i0 = 0;
    while ((i0 < 4)) {
        n.p.x = (((("hello")[2..4])[0..2])[1] + (str_eq(("abcd")[1..3], sv1) >= ((("hello")[2..5]).len / ((127 & 15) + 1))));
        a[i0] = 989;
        i0 = (i0 + 1);
    }
    v0 = (((add2(v2, v1) as u32) as u8) * str_eq(("abcd")[1..3], ("xyzzy")[0..2]));
    n.p.x = (((-(0.25 as i64)) >> '1') ^ (((!'0') >> (("hello")[1..3])[1]) as i8));
    p.y = (-(-311 + tag_e(e)));
    i0 = 0;
    while ((i0 < 4)) {
        a[((sv0)[0] & 3)] = 100;
        i1 = 0;
        while ((i1 < 2)) {
            i1 = (i1 + 1);
            n.tag = (((str_eq("xyz", "foobar") as double) / 1.7) as i64);
            v2 = v1;
        }
        a[i0] = (p.x % (((((-1 % ((true & 15) + 1)) | (true != 160)) >= (-('\t' as u32))) & 15) + 1));
        i0 = (i0 + 1);
    }
    sv1 = "foobar";
    match (e) {
        A => {
            return v2;
        }
        B(mb0) => {
            return ((!(!((-1 & false) != (-787 >> 3)))) - mb0);
        }
        C(mb0, mb1) => {
            return (zero() & mb1);
        }
    }
    v0, v2 = v2, v0;
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (("no")[0..2]).len;
}
