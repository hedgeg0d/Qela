// expect-exit: 254
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 12;
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn mul2(a int, b int) int { return a * b; }
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 9;
    var v1 int = 2;
    var v2 int = 7;
    var sv0 str = "abcd";
    var sv1 str = "abcd";
    var fv double = 1.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    n.p.x = (((("xyz")[0..2])[0..2])[0..2]).len;
    match (e) {
        A => {
            return ((-(2.0 as i64)) as i8);
        }
        B(mb0) => {
            return (tag_e(e) * mb0);
        }
        C(mb0, mb1) => {
            return (((((!true) >> (127 - false)) > ("hi")[1]) << ('\t' as u8)) + mb1);
        }
    }
    gv, v2 = v2, gv;
    gv, v2 = v2, gv;
    v0 = ((((10 as u16) as i8) < (("abcd")[1..3]).len) > '1');
    i0 = 0;
    while ((i0 < 1)) {
        i0 = (i0 + 1);
        gv = ((v0 % ((gv & 15) + 1)) >> (((gv > '0') ^ v1) ^ -257));
        e = E.B((sv0).len);
    }
    v2 = (p.y | p.y);
    v2, v1 = v1, v2;
    p.x = (((str_eq((("abcd")[1..4])[0..2], (sv1)[0..2]) as i8) as u16) ^ 'c');
    n.tag = (a[((a[('d' & 3)] >= (v1 ^ true)) & 3)] <= ((("foobar")[3..5])[0] != (-('c' % ((true & 15) + 1)))));
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (fv != ((fv / 10.5) + -1.5));
}
