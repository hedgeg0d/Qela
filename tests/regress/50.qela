// expect-exit: 1
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn min2(a int, b int) int { if (a < b) { return a; } else { return b; } }
fn sum3(a int, b int, c int) int { return a + b + c; }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 5;
    var v1 int = 1;
    var v2 int = 5;
    var sv0 str = "hi";
    var sv1 str = "abcd";
    var fv double = 0.25;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    match (e) {
        A => {
            return ((-((-false) == ('e' <= -589))) <= (0.125 as i64));
        }
        B(mb0) => {
            return ("xyzzy").len;
        }
        C(mb0, mb1) => {
            return (wide_sum(w) & mb1);
        }
    }
    fv = ((fv * 0.1) + 1.7);
    a[((fv <= (tag_e(e) as double)) & 3)] = ((100.0 < (v1 as double)) < ((a[(-258 & 3)] <= (!'0')) == ("hello")[0]));
    match (e) {
        A => {
            return ((-("abc")[0]) >= v0);
        }
        B(mb0) => {
            return (-708 + mb0);
        }
        C(mb0, mb1) => {
            return (v0 ^ mb1);
        }
    }
    sv1 = ("abcd")[1..3];
    v0, v2 = (v2 < (((-943 & 146) | p.y) - v1)), ((!((v1 >> 893) >> (3 >> v2))) as u8);
    v0, v2 = str_eq(("xyz")[0..2], ("hello")[1..4]), (10.5 as i64);
    a[(true & 3)] = ((((1 - true) > tag_e(e)) as u8) << 'd');
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return str_eq(("abc")[0..3], sv0);
}
