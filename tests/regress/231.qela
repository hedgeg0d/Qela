// expect-exit: 1
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -35;
fn mul2(a int, b int) int { return a * b; }
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 0;
    var v1 int = 2;
    var v2 int = 0x5;
    var sv0 str = "qwerty";
    var sv1 str = "qwerty";
    var fv double = 0.1;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    v2 = (str_eq(sv1, (sv1 + ("xyzzy")[1..3])) - (str_eq("aa", ("hello")[0..4]) in 0..((((true + v1) - (false < 700)) & 15) + 1)));
    p.y = mul2(a[((true + a[('f' & 3)]) & 3)], ((v2 in 0..((v0 & 15) + 1)) != a[((!255) & 3)]));
    gv = (sv1).len;
    match (e) {
        A => {
            return ((((gv as u16) ^ (466 - -1)) in a) in a);
        }
        B(mb0) => {
            return (!((((gv > 907) && (907 > 3)) << (641 + 0xff)) < (false <= (1.5 as i64))));
        }
        C(mb0, mb1) => {
            return (((!(--1)) in 0..((((((-v2) >= diff4(gv, v2, true, 1)) && (diff4(gv, v2, true, 1) >= (false ^ 42))) / (((pair_sum(Pair{x: false, y: 'b'}) != ('c' as i32)) & 15) + 1)) & 0xf) + 1)) + mb1);
        }
    }
    v2 = ("xyzzy").len;
    v0 = 10;
    e = E.B((p.x as i32));
    gv = ((((fv / 0.125) + (2.5 - 2.5)) - ((10 ^ 'e') as double)) < (1.5 / 0.3));
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((!((255 > 'a') in 0..((((42 < v1) && (v1 < 100)) & 15) + 1))) + (!mul2((-gv), (false - '3'))));
}
