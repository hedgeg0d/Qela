// expect-exit: 3
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 23;
fn add2(a int, b int) int { return a + b; }
fn twice(n int) int { return n + n; }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn zero() int { return 3; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 3;
    var v1 int = 0;
    var v2 int = 9;
    var sv0 str = "abcd";
    var sv1 str = "abc";
    var fv double = 2.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    n.tag = a[(((-(255 | 597)) ^ (sv0).len) & 3)];
    n.tag = false;
    i0 = 0;
    while ((i0 < 1)) {
        i0 = (i0 + 1);
        n.tag = (((add2(2, false) != (true as u16)) as u16) + v2);
        gv = ((((gv < 0) as double) as i64) | (((sv0)[0..2]).len > (fv as i64)));
        i1 = 0;
        while ((i1 < 3)) {
            i1 = (i1 + 1);
            v2 = (-(sv0)[0]);
            if ((i1 != 0)) {
                continue;
            } else {
            }
        }
        if ((i0 != 0)) {
            continue;
        } else {
        }
    }
    gv = ((a[((true << gv) & 3)] as double) == fv);
    match (e) {
        A => {
            return 3;
        }
        B(mb0) => {
            return ((!'\t') + mb0);
        }
        C(mb0, mb1) => {
            return ('b' | mb1);
        }
    }
    match (e) {
        A => {
            return ((!(!('a' + 'b'))) | a[((('c' < gv) >> (0.3 == fv)) & 3)]);
        }
        B(mb0) => {
            return (((str_eq(("xyz")[0..3], "xyzzy") & p.y) / ((((twice('\n') - (-true)) - (-(604 < false))) & 15) + 1)) & mb0);
        }
        C(mb0, mb1) => {
            return '0';
        }
    }
    p.x = (gv < ((-789 as u16) >= (("qwerty")[2..4])[1]));
    n.tag = ((((v0 | 2) ^ (1.5 == fv)) & ((1.7 == fv) + opt_d(D.Some(976)))) / (((mix6(738, true, (-1.5 as i64), (-847 <= 0), (gv >> true), a[('\t' & 3)]) / (((('g' % (('\t' & 15) + 1)) ^ (10 % ((255 & 15) + 1))) & 15) + 1)) & 15) + 1));
    n = Nest{tag: ((v1 as u16) as i8), p: Pair{x: 'c', y: (-((3.75 + fv) as i64))}};
    if ((false < p.x)) {
        w = wide_swap(w);
        return (-1 / (((!((3.75 - 0.5) <= (gv as double))) & 15) + 1));
    } else {
        gv, v2 = v2, gv;
    }
    gv = ((1.5 != fv) - (p.y >> 3));
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return p.y;
}
