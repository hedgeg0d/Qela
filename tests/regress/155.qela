// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -20;
fn zero() int { return 3; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn min2(a int, b int) int { if (a < b) { return a; } else { return b; } }
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 10;
    var v1 int = 1;
    var v2 int = 1;
    var sv0 str = "foobar";
    var sv1 str = "abc";
    var fv double = -1.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    v0 = v0;
    gv = (((('f' >> true) as u16) as u32) & (((-634 as u8) >= (-270)) as u32));
    sv0 = ("abcd")[1..3];
    n.p.x = v2;
    i0 = 0;
    while ((i0 < 4)) {
        match (e) {
            A => {
                return p.x;
            }
            B(mb0) => {
                return ((p.x < ((!(("xyzzy")[2..5]).len) < diff4((10 / ((true & 15) + 1)), (!gv), 943, p.y))) & mb0);
            }
            C(mb0, mb1) => {
                return (zero() * mb1);
            }
        }
        a[i0] = ((pair_sum(Pair{x: v0, y: 'a'}) ^ (-(260 != 192))) <= ((((("abcd")[1..4])[0..2])[1] << (v1 != 1)) <= (diff4('2', 42, v2, 136) as i16)));
        i0 = (i0 + 1);
    }
    v1 += (nest_sum(n) as i32);
    e = E.C((!str_eq(("abcd")[1..4], (sv0)[0..2])), (!((gv + (v1 != 972)) * (str_eq(sv1, "abcd") as u8))));
    v1 ^= ((p.x ^ ((gv >> 287) >> ('\n' >> false))) ^ min2(((692 % ((255 & 15) + 1)) <= (!-30)), v1));
    match (e) {
        A => {
            return 'c';
        }
        B(mb0) => {
            return (((((!'d') as u16) as u32) >= (a[(('a' <= v1) & 3)] <= ((gv > 2) >> (319 * true)))) ^ mb0);
        }
        C(mb0, mb1) => {
            return ((sv1)[1] | mb1);
        }
    }
    sv1 = "abcd";
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((fv as i64) >= (931 > '\n')) as i16) + (((gv >> gv) << (gv - v2)) > (-(-1 + '0'))));
}
