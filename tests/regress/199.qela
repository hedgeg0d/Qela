// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 50;
fn fact(n int) int { if (n <= 1) { return 1; } return n * fact(n - 1); }
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn min2(a int, b int) int { if (a < b) { return a; } else { return b; } }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 10;
    var v1 int = 6;
    var v2 int = 7;
    var sv0 str = "foobar";
    var sv1 str = "abcd";
    var fv double = -2.0;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    fv = (((sv0)[1] < ((995 % ((634 & 15) + 1)) <= (10 + '\t'))) as double);
    v0 = gv;
    i0 = 0;
    while ((i0 < 3)) {
        i0 = (i0 + 1);
        match (e) {
            A => {
                return (str_eq(("xyz")[0..3], ("abcd")[1..3]) ^ str_eq("xyzzy", "xyz"));
            }
            B(mb0) => {
                return ((-min2(((false % ((true & 15) + 1)) as u32), ((862 != -175) % (((false <= 10) & 15) + 1)))) * mb0);
            }
            C(mb0, mb1) => {
                return (((((598 <= false) << -987) as i16) >> ((min2(891, '\n') + (-1 / ((v0 & 15) + 1))) > (str_eq(("abc")[0..2], sv1) as u8))) * mb1);
            }
        }
    }
    gv = tag_e(e);
    fv = ((((1.5 - fv) - (0.1 / 1.0)) * (('0' as double) - (0.25 * fv))) / 0.25);
    v0 <<= ('e' ^ (((1 as i32) == (v1 as i16)) as u16));
    a[((str_eq("zz", sv0) >= (-1)) & 3)] = tag_e(e);
    v1 &= v0;
    fv = -2.0;
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (((false + -256) & ((10 / ((3 & 15) + 1)) < true)) * ((-(-1 & 866)) % ((((v2 / ((true & 15) + 1)) * v1) & 15) + 1)));
}
