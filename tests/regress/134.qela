// expect-exit: 8
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn zero() int { return 3; }
fn max2(a int, b int) int { if (a > b) { return a; } else { return b; } }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 9;
    var v1 int = 8;
    var v2 int = 7;
    var sv0 str = "abc";
    var sv1 str = "hello";
    var fv double = 10.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    v0 >>= ((((v0 >> 3) != str_eq("xyz", "hello")) >> (("xyzzy")[0] == (-false))) < (a[((1 >> 1) & 3)] / ((((v0 % ((v1 & 15) + 1)) | ('\n' as i32)) & 15) + 1)));
    if (((sv1)[0] <= ((-wide_sum(w)) >= 125))) {
        sv1 = (("foobar")[1..5])[0..2];
    } else {
        v0 = (!((0.1 as i64) as i8));
    }
    match (e) {
        A => {
            return v1;
        }
        B(mb0) => {
            return ((((fv == (523 as double)) > ((582 % ((-461 & 15) + 1)) as u16)) / (((!(!('2' >> '\n'))) & 15) + 1)) + mb0);
        }
        C(mb0, mb1) => {
            return (((-("qwerty")[0]) == 3) - v1);
        }
    }
    fv = ((((--912) + v0) as double) / 0.125);
    sv1 = "aa";
    i0 = 0;
    while ((i0 < 4)) {
        a[((((sv0)[0] as u8) | (('1' <= (v2 > -376)) << (('\n' as u16) > (204 + v2)))) & 3)] = (("hello")[2..4]).len;
        a[i0] = (str_eq((("42")[0..2])[0..2], ("qwerty")[0..6]) as i32);
        i0 = (i0 + 1);
    }
    p.x = 771;
    n.p.x = (("abcd")[1..3])[0];
    e = E.C((str_eq(("abc")[0..3], ("hello")[0..4]) | ((("abcd")[0..2])[0..2]).len), (v1 % (((-(('c' <= -542) - v0)) & 15) + 1)));
    if ((((0.5 < fv) ^ ((('1' as u32) > (!v1)) & ((!'f') >> true))) > ((((255 | 'c') * ('1' as u16)) << p.y) > (1 as u8)))) {
        v0 %= ((((-((false as i16) - (-33 == 255))) >> v2) & 15) + 1);
        n.tag = (100 & v2);
        return ((((0.125 / 2.5) == 0.25) >> (p.y >= str_eq(sv0, (((sv1)[0..2])[0..2])[0..2]))) - ((1 - pair_sum(p)) as i16));
    } else {
        v0 += (-str_eq(("foobar")[1..6], sv1));
        v2 = 600;
    }
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return '3';
}
