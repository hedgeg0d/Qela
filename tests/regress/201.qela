// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 22;
fn fact(n int) int { if (n <= 1) { return 1; } return n * fact(n - 1); }
fn sum3(a int, b int, c int) int { return a + b + c; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 7;
    var v1 int = 0;
    var v2 int = 8;
    var sv0 str = "abcd";
    var sv1 str = "abcd";
    var fv double = 0.25;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    gv <<= ((!((0.1 == fv) as u16)) ^ (((557 as double) as i64) as u32));
    v0, v2 = v2, v0;
    v1 = 'g';
    v0 = (v2 as i16);
    v1 = (v1 % (((((fv / 0.5) / 1.5) <= ((1 as double) * fv)) & 15) + 1));
    a[(-552 & 3)] = ((!((--563) <= -669)) as i16);
    v1 = (((1.7 * (2 as double)) + ((2.5 / -1.5) / -1.5)) < fv);
    sv0 = sv1;
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return (((0.5 / 1.7) as i64) != (((v1 / ((255 & 15) + 1)) | str_eq("42", "abcd")) as u16));
}
