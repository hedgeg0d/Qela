// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn zero() int { return 3; }
fn twice(n int) int { return n + n; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 3;
    var v1 int = 8;
    var v2 int = 3;
    var sv0 str = "qwerty";
    var sv1 str = "no";
    var fv double = -1.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    e = E.C((-(sv0)[1]), (a[(((3 != -950) as u32) & 3)] < 771));
    v2 = (!('\n' as i16));
    fv = (opt_d(D.None) as double);
    v1 = v1;
    n.tag = (twice((!v0)) < (((false >= v2) as u16) <= ((fv * fv) as i64)));
    e = E.C(((p.x as double) as i64), ((((-604 >> v0) <= p.x) >> (1.0 > fv)) as u32));
    i0 = 0;
    while ((i0 < 4)) {
        match (e) {
            A => {
                return v1;
            }
            B(mb0) => {
                return ((((fv / 2.5) < (fv + fv)) >> ((false << v1) as u32)) >= ((zero() as u8) << ((108 << 42) ^ -188)));
            }
            C(mb0, mb1) => {
                return (((fv + 3.75) == ((twice(v2) + (true * true)) as double)) + mb1);
            }
        }
        a[i0] = ((((false - -761) % (((1 as u16) & 15) + 1)) | ((sv0)[0..2]).len) >= v2);
        i0 = (i0 + 1);
    }
    n.tag = ((((--692) << (-1 / ((v1 & 15) + 1))) as double) < fv);
    e = E.C(((((-v2) / (((10 << v1) & 15) + 1)) as u8) as u16), ((a[((127 / ((v2 & 15) + 1)) & 3)] << opt_d(D.None)) - ((((("foobar")[1..3])[0..2])[1] ^ (true < 1)) > 145)));
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((-(!(589 / (('f' & 15) + 1)))) * str_eq(sv1, sv1));
}
