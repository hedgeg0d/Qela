// expect-exit: 1
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -14;
fn max2(a int, b int) int { if (a > b) { return a; } else { return b; } }
fn sub2(a int, b int) int { return a - b; }
fn zero() int { return 3; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 2;
    var v1 int = 7;
    var v2 int = 9;
    var sv0 str = "abcd";
    var sv1 str = "xyz";
    var fv double = 3.75;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    e = E.B(((0.25 / 0.125) != ((('\n' ^ v2) / (((v1 / (('f' & 15) + 1)) & 15) + 1)) as double)));
    fv = ((((fv as i64) as double) + ((-144 ^ 347) as double)) / -0.5);
    gv = (((gv <= ('c' / ((true & 15) + 1))) as double) as i64);
    fv = (10.5 * ((v2 ^ ('\n' <= v2)) as double));
    p.y = (false as i8);
    match (e) {
        A => {
            return tag_e(e);
        }
        B(mb0) => {
            return (((-1 >= 830) as u16) + mb0);
        }
        C(mb0, mb1) => {
            return ((-(-((gv * false) as i32))) ^ mb1);
        }
    }
    v0 = 'f';
    match (e) {
        A => {
            return ((((-414 + true) > (127 <= 'f')) as u8) >> (((-640 <= 883) as u32) ^ p.y));
        }
        B(mb0) => {
            return ((-(((-1) % ((('2' >= 127) & 15) + 1)) < ((-true) as u32))) * mb0);
        }
        C(mb0, mb1) => {
            return (nest_sum(Nest{tag: (-0.5 > (((-'f') % ((0 & 15) + 1)) as double)), p: Pair{x: ((!((-1 as double) as i64)) - ((('\n' as u8) != (-587)) % ((((fv as i64) >= (false * v0)) & 15) + 1))), y: gv}}) & mb1);
        }
    }
    p.x = ('2' - tag_e(e));
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return false;
}
