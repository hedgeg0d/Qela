// expect-exit: 1
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn twice(n int) int { return n + n; }
fn zero() int { return 3; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 0;
    var v1 int = 7;
    var v2 int = 8;
    var sv0 str = "no";
    var sv1 str = "42";
    var fv double = 1.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    e = E.C((!true), (((!('0' != v0)) > ((("qwerty")[1..5])[1] ^ (--1))) / (((-(a[(0 & 3)] + (-652 / ((-531 & 15) + 1)))) & 15) + 1)));
    v0 &= ((((906 % ((true & 15) + 1)) as i8) * (("hello")[2..5])[0]) as i32);
    p.y = (((true % ((twice(-1) & 15) + 1)) > ((fv == fv) | (!false))) <= (v2 | (!(v0 >= '\n'))));
    i0 = 0;
    while ((i0 < 4)) {
        v0 = ((((3 as i8) > (v1 * 0)) as u16) == (v1 == ((-992 as i8) ^ (!v0))));
        a[(a[(((-false) % ((((-'1') - (sv1)[0]) & 15) + 1)) & 3)] & 3)] = (-330 < (zero() + false));
        a[i0] = (a[(((fv / 2.5) >= fv) & 3)] * (((-v1) > (v1 ^ v2)) > (((("abc")[0..2])[0..2])[0..2]).len));
        i0 = (i0 + 1);
    }
    p.y = a[((((100 as u8) != (false < true)) != v1) & 3)];
    v1 >>= 100;
    v1 = twice((sv0).len);
    match (e) {
        A => {
            return 270;
        }
        B(mb0) => {
            return (pair_sum(Pair{x: ('d' >> (v2 * 'b')), y: (zero() as u32)}) - str_eq(sv0, ("qwerty")[2..6]));
        }
        C(mb0, mb1) => {
            return ((!v0) - mb1);
        }
    }
    v2 = (((('g' as i32) < 'c') as i16) != (v0 <= pair_sum(p)));
    match (e) {
        A => {
            return (opt_d(D.Some((-(("xyz")[0..3]).len))) % ((pair_sum(p) & 15) + 1));
        }
        B(mb0) => {
            return ((-493 >= ((v1 << true) as i16)) == ('b' < ("no")[1]));
        }
        C(mb0, mb1) => {
            return (-p.x);
        }
    }
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return -402;
}
