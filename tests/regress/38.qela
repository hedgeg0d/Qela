// expect-exit: 40
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 40;
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn sub2(a int, b int) int { return a - b; }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 5;
    var v1 int = 9;
    var v2 int = 4;
    var sv0 str = "no";
    var sv1 str = "42";
    var fv double = 0.3;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    match (e) {
        A => {
            return gv;
        }
        B(mb0) => {
            return (-a[((p.y + diff4(true, 127, -760, 'e')) & 3)]);
        }
        C(mb0, mb1) => {
            return ((((v2 != -939) + pair_sum(p)) / ((a[((v0 ^ true) & 3)] & 15) + 1)) as i32);
        }
    }
    match (e) {
        A => {
            return 'g';
        }
        B(mb0) => {
            return ((("xyzzy")[0..3])[0] - mb0);
        }
        C(mb0, mb1) => {
            return ((85 | (((0.25 < 2.5) << p.y) as u16)) & mb1);
        }
    }
    gv = (100.0 as i64);
    sv0 = ("xyz")[0..3];
    a[((((nest_sum(n) >= (true << -1)) as i16) ^ ((a[('a' & 3)] << (-249 * -1)) < (((sv0)[0..2])[0..2]).len)) & 3)] = ((((!'3') == ('b' != false)) as i32) & 887);
    a[(((p.y == (false & diff4(false, 624, v2, 100))) ^ (((fv / 10.5) > (10.5 * 0.5)) | (!(917 / ((100 & 15) + 1))))) & 3)] = ((("xyzzy")[2..4])[1] & v1);
    e = E.C(-427, (nest_sum(Nest{tag: (gv as i32), p: Pair{x: -346, y: 100}}) as u16));
    v0 = (false << (((-303 >> -845) / (((false + false) & 15) + 1)) ^ (false >> (true * -854))));
    p.y = false;
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((!'c') + v0) as u16) | (0.3 as i64));
}
