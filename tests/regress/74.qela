// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -15;
fn zero() int { return 3; }
fn sub2(a int, b int) int { return a - b; }
fn sum3(a int, b int, c int) int { return a + b + c; }
fn fact(n int) int { if (n <= 1) { return 1; } return n * fact(n - 1); }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn bag_sum(b Bag) int { return b.p + b.q + b.r + b.s; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 8;
    var v1 int = 9;
    var v2 int = 0;
    var sv0 str = "zz";
    var sv1 str = "foobar";
    var fv double = 1.0;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    match (e) {
        A => {
            return ((-'3') > (("hello")[1..4]).len);
        }
        B(mb0) => {
            return (("zz").len + mb0);
        }
        C(mb0, mb1) => {
            return 413;
        }
    }
    match (e) {
        A => {
            return a[((((!'2') | (sv1).len) as i8) & 3)];
        }
        B(mb0) => {
            return (((("xyzzy")[0..4])[0..2])[0] ^ fact((sub2((gv % ((-411 & 15) + 1)), (-595 == 42)) & 15)));
        }
        C(mb0, mb1) => {
            return ((("hello")[0..4]).len + mb1);
        }
    }
    i0 = 0;
    while ((i0 < 3)) {
        i0 = (i0 + 1);
        a[(((0.3 - 0.25) as i64) & 3)] = (("abcd")[0..4])[1];
        match (e) {
            A => {
                return gv;
            }
            B(mb0) => {
                return (((fv as i64) > str_eq(("hello")[2..4], sv1)) as u32);
            }
            C(mb0, mb1) => {
                return (((((1 / ((gv & 15) + 1)) & (false & v2)) * (-(--656))) & (true | ((fv != 1.0) ^ (234 as u8)))) | mb1);
            }
        }
        v1, v2 = v2, v1;
        if ((i0 == 1)) {
            break;
        } else {
        }
    }
    v2 = (((gv as u32) & ("hi")[1]) > (-((gv >= gv) as u32)));
    v1 = ('d' >> p.x);
    match (e) {
        A => {
            return (((2.5 * -1.5) as i64) + opt_d(D.Some((p.x - (!v1)))));
        }
        B(mb0) => {
            return p.x;
        }
        C(mb0, mb1) => {
            return (('c' < ((fv != ('\n' as double)) >= ((!'g') < (!10)))) & mb1);
        }
    }
    sv0 = (("xyzzy")[1..3])[0..2];
    if (((v1 < ((fact((v0 & 15)) == (2.5 <= 0.25)) & ((fv > fv) ^ (!'b')))) == str_eq("abcd", (("foobar")[1..6])[0..2]))) {
        sv1 = sv1;
        return (v1 - (((v2 as u8) % (('0' & 15) + 1)) > (str_eq(sv1, sv1) as i32)));
    } else {
        if ((191 <= (-wide_sum(w)))) {
            if ((('1' - (!v0)) <= (((sv0)[1] as double) as i64))) {
                match (e) {
                    A => {
                        return ("hello").len;
                    }
                    B(mb0) => {
                        return 100;
                    }
                    C(mb0, mb1) => {
                        return (((!(str_eq(("xyzzy")[0..4], ("abcd")[0..2]) + ('0' < 42))) as i16) & mb1);
                    }
                }
            } else {
                n.tag = true;
                w = Wide{a: str_eq(sv1, sv0), b: (sv0)[1], c: (("hello")[2..4]).len};
                return ((-p.x) < (((fv / 0.3) != fv) < sum3((false | true), (0 as u32), ("abcd")[0])));
            }
        } else {
            i2 = 0;
            while ((i2 < 1)) {
                i2 = (i2 + 1);
                v0 -= pair_sum(p);
                w = Wide{a: ((42 <= ((("abc")[0..3]).len + ('1' as i32))) as u32), b: p.x, c: str_eq("qwerty", ((("hello")[2..4])[0..2])[0..2])};
            }
        }
    }
    n.tag = (-str_eq("aa", "no"));
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((-((-85 == '0') as u16)) - (((1.7 * fv) >= fv) > ((sv1)[0] <= ('1' * -1))));
}
