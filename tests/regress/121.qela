// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
fn max2(a int, b int) int { if (a > b) { return a; } else { return b; } }
fn mix5(a int, b int, c int, d int, e int) int { return a + b - c + d - e; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 0;
    var v1 int = 7;
    var v2 int = 6;
    var sv0 str = "hi";
    var sv1 str = "no";
    var fv double = -1.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    p.x = (-(((0.5 - fv) as i64) >> (!(("xyzzy")[0..2])[0])));
    match (e) {
        A => {
            return str_eq((("qwerty")[0..6])[0..2], sv0);
        }
        B(mb0) => {
            return -578;
        }
        C(mb0, mb1) => {
            return (((p.y / ((('\t' < 'g') & 15) + 1)) ^ ((fv / 1.5) > (fv * fv))) << ((fv < fv) as u8));
        }
    }
    fv = (fv / 0.3);
    p = pair_swap(p);
    match (e) {
        A => {
            return 255;
        }
        B(mb0) => {
            return ((((!(255 + false)) >= (10 ^ (true | true))) as i16) - mb0);
        }
        C(mb0, mb1) => {
            return (((("foobar")[3..6])[0] | (sv1)[0]) ^ mb1);
        }
    }
    a[(('\t' as u16) & 3)] = a[((((-907) ^ (-v1)) == p.x) & 3)];
    p.y = ((v0 == (100 % ((v0 & 15) + 1))) as u32);
    v1 = v2;
    v2 = (((!(((("hello")[0..4])[0..2])[0..2]).len) <= (fv as i64)) >> (-((false as double) as i64)));
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ('0' * (((1.5 + 3.75) / 100.0) as i64));
}
