// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -38;
fn add2(a int, b int) int { return a + b; }
fn twice(n int) int { return n + n; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn wide_make(a int, b int, c int) Wide { return Wide{a: a, b: b, c: c}; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 2;
    var v1 int = 1;
    var v2 int = 1;
    var sv0 str = "foobar";
    var sv1 str = "zz";
    var fv double = 0.1;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    sv0 = ("xyzzy")[0..4];
    v1 = ((((sv0)[0] <= (2 == 936)) | ((true as u32) as u16)) ^ 866);
    v1 = (add2((a[('e' & 3)] | (-1 != 417)), twice(false)) % ((((860 as u8) as u16) & 15) + 1));
    a[((sv1)[0] & 3)] = 3;
    v0, gv = gv, v0;
    if (((0 as u16) >= (-(((10.5 / -2.0) / 1.7) as i64)))) {
        gv = v2;
        return ((((false + 521) as i32) as u32) ^ gv);
    } else {
        gv = (10 ^ (-((!true) <= a[(v0 & 3)])));
    }
    p.y = false;
    v0 = (((sv1)[0..2]).len / ((p.y & 15) + 1));
    p.x = pair_sum(p);
    w = wide_make(((add2((0 == false), ('0' as u32)) / ((((-106 as u16) as u16) & 15) + 1)) as i16), ((-17 >> ((sv0)[0] - (fv <= -2.0))) > add2(gv, (true % ((v0 & 15) + 1)))), ((0.5 as i64) ^ (0.25 as i64)));
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((-'2') == nest_sum(Nest{tag: '0', p: Pair{x: (1 ^ (!(-(gv | true)))), y: v0}})) <= ((-1.5 / 1.5) as i64)) / (((("abcd")[1..3]).len & 15) + 1));
}
