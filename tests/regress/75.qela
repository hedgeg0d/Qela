// expect-exit: 42
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 42;
fn mix5(a int, b int, c int, d int, e int) int { return a + b - c + d - e; }
fn diff4(a int, b int, c int, d int) int { return a - b - c - d; }
fn twice(n int) int { return n + n; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn main() int {
    var v0 int = 10;
    var v1 int = 5;
    var v2 int = 9;
    var sv0 str = "aa";
    var sv1 str = "no";
    var fv double = -0.5;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    v2 = (-'c');
    v2 *= (-(((2.0 > fv) << 100) - -447));
    a[((a[((gv != (gv as i16)) & 3)] % (((((!'\n') == p.y) >> ((v0 + gv) + v1)) & 15) + 1)) & 3)] = (((v1 as u16) <= ((-1 as double) > 0.3)) + (wide_sum(w) as i16));
    match (e) {
        A => {
            return ((((!gv) as u16) | gv) | (((false + '\t') - (!false)) as i8));
        }
        B(mb0) => {
            return a[(p.x & 3)];
        }
        C(mb0, mb1) => {
            return ((((gv as i16) == ("abcd").len) >= ((-215 % ((-678 & 15) + 1)) != ((("xyzzy")[0..3])[0..2])[1])) + p.y);
        }
    }
    p.x = 523;
    p.x = (wide_sum(w) ^ '\n');
    v1 = ((-str_eq("xyzzy", sv1)) as u8);
    a[((gv / ((((fv as i64) + ((-gv) as i8)) & 15) + 1)) & 3)] = 'c';
    v1 = ((!262) % ((true & 15) + 1));
    n = Nest{tag: p.x, p: Pair{x: (-wide_sum(w)), y: ((((0.25 as i64) ^ 10) < ((sv0).len * (fv == fv))) & (v2 as i32))}};
    match (e) {
        A => {
            return (p.y % (((("xyz")[0..2]).len & 15) + 1));
        }
        B(mb0) => {
            return (a[(((-(-1 & 496)) % (((("aa").len - (gv & '\t')) & 15) + 1)) & 3)] & mb0);
        }
        C(mb0, mb1) => {
            return (((gv % ((str_eq("no", ((("abc")[0..2])[0..2])[0..2]) & 15) + 1)) as i8) * mb1);
        }
    }
    var ob Buf;
    fmt_i64(&ob, v1);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((100 as double) != ('2' as double)) >> 993) >= ((-gv) == (mix5(gv, '0', -141, 417, -509) >> (-true))));
}
