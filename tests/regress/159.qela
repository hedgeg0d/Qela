// expect-exit: 0
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = -1;
fn mul2(a int, b int) int { return a * b; }
fn mix6(a int, b int, c int, d int, e int, f int) int { return a - b + c - d + e - f; }
fn twice(n int) int { return n + n; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn wide_sum(w Wide) int { return w.a + w.b + w.c; }
fn opt_d(d D) int {
	match (d) {
		None => { return 0; }
		Some(v) => { return v; }
	}
}
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 3;
    var v1 int = 7;
    var v2 int = 6;
    var sv0 str = "aa";
    var sv1 str = "qwerty";
    var fv double = 0.125;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    p.x = (sv1)[1];
    a[(2 & 3)] = (v2 >= str_eq(sv0, sv0));
    gv = a[(false & 3)];
    v2 = p.x;
    n.p.x = (!(-'g'));
    i0 = 0;
    while ((i0 < 4)) {
        a[(((((2.5 + fv) as i64) <= ((false < -760) >= (361 | -537))) / (((((fv * fv) != (0.25 / 3.75)) != ((gv <= -511) % (((583 != 255) & 15) + 1))) & 15) + 1)) & 3)] = 'd';
        a[i0] = (p.y as u16);
        i0 = (i0 + 1);
    }
    if (((((v0 ^ (true < 1)) >= true) <= 255) >= (((("hello")[2..4])[0..2]).len <= ((2 + (gv / ((0 & 15) + 1))) % ((((42 / ((false & 15) + 1)) << (-1 & 459)) & 15) + 1))))) {
        e = E.B(((fv > (fv + (-250 as double))) > a[((str_eq("no", ("xyz")[0..3]) % ((('3' & false) & 15) + 1)) & 3)]));
    } else {
        a[(("foobar").len & 3)] = ((!str_eq((("foobar")[0..3])[0..2], ("xyz")[0..3])) * wide_sum(w));
        gv = (!mul2(((-565 + '0') >= (!10)), ((2 > true) % (((10 as u32) & 15) + 1))));
    }
    v0 = a[(str_eq(("hello")[0..2], ("xyz")[0..3]) & 3)];
    gv = (sv1)[1];
    v2 %= ((twice((-((-1) % (((sv0)[1] & 15) + 1)))) & 15) + 1);
    var ob Buf;
    fmt_i64(&ob, v0);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((fv == 0.1) / (((1.5 <= fv) & 15) + 1)) as u32) & ((!(v1 & 747)) + (-str_eq("abc", "qwerty"))));
}
