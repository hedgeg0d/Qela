// expect-exit: 4
// got: CRASH
struct Pair { x int, y int, }
struct Wide { a int, b int, c int, }
struct Bag { p int, q int, r int, s int, }
struct Nest { tag int, p Pair, }
enum E { A, B(int), C(int, int), }
enum D { None, Some(int), }
import "std/fmt.qela";
import "std/io.qela";
import "std/str.qela";
var gv int = 4;
fn sum3(a int, b int, c int) int { return a + b + c; }
fn twice(n int) int { return n + n; }
fn gcd2(a int, b int) int { while (b != 0) { var t int = a % b; a = b; b = t; } return a; }
fn pair_sum(p Pair) int { return p.x + p.y; }
fn pair_swap(p Pair) Pair { return Pair{x: p.y, y: p.x}; }
fn wide_swap(w Wide) Wide { return Wide{a: w.b, b: w.a, c: w.c}; }
fn nest_sum(n Nest) int { return n.tag + n.p.x + n.p.y; }
fn tag_e(e E) int {
	match (e) {
		A => { return 7; }
		B(v) => { return v + 1; }
		C(x, y) => { return x * 2 + y; }
	}
}
fn main() int {
    var v0 int = 2;
    var v1 int = 1;
    var v2 int = 4;
    var sv0 str = "xyz";
    var sv1 str = "abc";
    var fv double = 1.7;
    var p Pair = Pair{};
    var w Wide = Wide{};
    var b Bag = Bag{};
    var n Nest = Nest{tag: 0, p: Pair{x: 0, y: 0}};
    var e E = E.A;
    var a [4]int;
    var i0 int = 0;
    var i1 int = 0;
    var i2 int = 0;
    var i3 int = 0;
    var i4 int = 0;
    var i5 int = 0;
    match (e) {
        A => {
            return gv;
        }
        B(mb0) => {
            return (("zz").len * mb0);
        }
        C(mb0, mb1) => {
            return ((tag_e(e) as i32) - mb1);
        }
    }
    gv = (((("abcd")[0] >> (0 | -794)) < (!(746 > false))) >= (((127 <= '1') < nest_sum(n)) as i16));
    if (((gv as u32) < ((-(335 >> (true % ((gv & 15) + 1)))) <= (!((-853 / ((3 & 15) + 1)) as u32))))) {
        gv, v0 = ((false + str_eq(sv1, ("abcd")[1..3])) % ((gcd2((((-1) as i32) & 15), (-141 & 15)) & 15) + 1)), str_eq(("abc")[0..3], sv1);
        v1 = (-((a[(-183 & 3)] & (42 / (('g' & 15) + 1))) == ((-1 - 42) <= 2)));
    } else {
        a[(((true != (('a' <= false) << v2)) ^ (((--490) as i16) as i8)) & 3)] = ((((false != true) as double) < (fv / 3.75)) % (('f' & 15) + 1));
        fv = (((('3' as u8) >= (-89 + 10)) >> gv) as double);
        return tag_e(e);
    }
    sv1 = "hi";
    v1 = (("42")[0] | (((fv * 100.0) >= (fv / 2.0)) << ((fv > fv) ^ false)));
    v2 = (fv == (((fv * 1.7) / 1.7) / 1.7));
    a[((str_eq(sv0, sv1) > (a[((v2 << 1) & 3)] > v2)) & 3)] = ((2.5 / 2.5) as i64);
    p = pair_swap(p);
    n.tag = ((sv0)[0..2])[0];
    v1, v2 = v2, v1;
    var ob Buf;
    fmt_i64(&ob, v2);
    write_str(STDOUT, buf_str(&ob));
    write_str(STDOUT, "\n");
    return ((((-234 as u32) as double) * (fv / 0.5)) as i64);
}
