// expect-compile-error
// A comptime block cannot produce a string: there is no constant slot to
// carry it in, and the previous behaviour emitted a garbage pointer.
fn main() str {
	var s = comptime { return "abc"; };
	return s;
}
