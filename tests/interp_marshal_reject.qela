// expect-compile-error
// stage1-only
// An enum whose payload holds a pointer-bearing type (a str) cannot cross
// the interpreted/dynamic ABI: its raw image would carry a host address.
enum E { Some(str), None }

fn dynamic bad(o E) i64 { return 0; }
fn main() int { return 0; }
