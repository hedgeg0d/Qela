// expect-compile-error
// stage1-only
// A payload enum cannot cross the interpreted/dynamic ABI (only scalars,
// str, floats and structs of those can).
enum Opt { Some(x i64), None }

fn dynamic bad(o Opt) i64 { return 0; }
fn main() int { return 0; }
