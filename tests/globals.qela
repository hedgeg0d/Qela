// expect-exit: 57
var counter int = 0;
var flags u8 = 0xf0;
let limit = 5 * 2;
var zeroed i32;

fn bump() int {
	counter += 3;
	return counter;
}

fn main() int {
	if (zeroed != 0) { return 1; }
	for i in 0..limit {
		bump();
	}
	flags |= 0x0f;
	return counter + (flags as int) - 228;
}
